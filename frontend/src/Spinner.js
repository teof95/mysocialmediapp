import React from "react";
import Loader from "react-loader-spinner";

const Spinner = () => {
  return (
<Loader type="BallTriangle" color="#ef1008" height={80} width={80} />
  );
};

export default Spinner;
